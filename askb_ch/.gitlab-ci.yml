include:
  - project: 'pkd/ci'
    file: 'dbt-ci.yml'
    ref: '$CI_COMMIT_REF_NAME'
    rules:
      - if: '$CI_COMMIT_BRANCH == "master"'
  - project: 'pkd/ci'
    file: 'dbt-ci.yml'
    ref: 'dev'
    rules:
      - if: '$CI_COMMIT_BRANCH != "master"'
